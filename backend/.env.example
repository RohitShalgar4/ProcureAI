# ============================================
# SERVER CONFIGURATION
# ============================================

# Port number for the backend server
# Default: 5000
PORT=5000

# Environment mode (development, production, test)
# Affects logging and error handling behavior
NODE_ENV=development

# ============================================
# DATABASE CONFIGURATION
# ============================================

# MongoDB connection string
# Local: mongodb://localhost:27017/rfp-management-system
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/rfp-management-system
# Required: Yes
MONGO_URI=mongodb://localhost:27017/rfp-management-system

# ============================================
# OPENAI CONFIGURATION
# ============================================

# OpenAI API key for AI features (RFP structuring, proposal parsing, comparison)
# Get your API key from: https://platform.openai.com/api-keys
# Required: Yes (for AI features)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI model to use
# Options: gpt-4-turbo-preview (recommended), gpt-4, gpt-3.5-turbo (faster/cheaper)
# Default: gpt-4-turbo-preview
OPENAI_MODEL=gpt-4-turbo-preview

# ============================================
# EMAIL CONFIGURATION (SMTP - Sending)
# ============================================

# Email service provider
# Options: gmail, sendgrid, ses, smtp (generic)
EMAIL_SERVICE=gmail

# SMTP host (for Gmail: smtp.gmail.com)
EMAIL_HOST=smtp.gmail.com

# SMTP port (587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Use SSL/TLS (false for STARTTLS on port 587, true for SSL on port 465)
EMAIL_SECURE=false

# Email address to send from
# For Gmail: your_email@gmail.com
# Required: Yes (for sending RFPs)
EMAIL_USER=your_email@gmail.com

# Email password or app-specific password
# For Gmail: Generate app-specific password at https://myaccount.google.com/apppasswords
# Required: Yes (for sending RFPs)
EMAIL_PASSWORD=your_app_specific_password_here

# ============================================
# ALTERNATIVE: SENDGRID CONFIGURATION
# ============================================
# Uncomment these lines to use SendGrid instead of Gmail

# EMAIL_SERVICE=sendgrid
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# Get API key from: https://app.sendgrid.com/settings/api_keys

# ============================================
# ALTERNATIVE: AWS SES CONFIGURATION
# ============================================
# Uncomment these lines to use AWS SES instead of Gmail

# EMAIL_SERVICE=ses
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key_here
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
# Get credentials from: https://console.aws.amazon.com/iam/

# ============================================
# CORS CONFIGURATION
# ============================================

# Frontend URL for CORS policy
# Development: http://localhost:5173
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:5173

# ============================================
# EMAIL RECEIVING CONFIGURATION (IMAP)
# ============================================

# IMAP host for receiving vendor responses
# For Gmail: imap.gmail.com
IMAP_HOST=imap.gmail.com

# IMAP port (993 for SSL/TLS)
IMAP_PORT=993

# Email address to receive vendor responses
# Should match EMAIL_USER for Gmail
# Required: Yes (for receiving proposals)
IMAP_USER=your_email@gmail.com

# IMAP password (same as EMAIL_PASSWORD for Gmail)
# For Gmail: Use the same app-specific password
# Required: Yes (for receiving proposals)
IMAP_PASSWORD=your_app_specific_password_here

# Use TLS for IMAP connection
IMAP_TLS=true

# ============================================
# EMAIL POLLING CONFIGURATION
# ============================================

# Enable automatic email polling to check for vendor responses
# Set to true to enable, false to disable
# Default: false (manual fetch only)
ENABLE_EMAIL_POLLING=false

# Interval in minutes between email polling checks
# Recommended: 5 (checks every 5 minutes)
# Lower values increase API usage but provide faster response detection
EMAIL_POLLING_INTERVAL=5

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. MongoDB Setup:
#    - Install MongoDB locally or create a free cluster at https://www.mongodb.com/cloud/atlas
#    - Update MONGO_URI with your connection string
#
# 2. OpenAI Setup:
#    - Sign up at https://platform.openai.com/
#    - Create an API key at https://platform.openai.com/api-keys
#    - Add credits to your account (pay-as-you-go)
#    - Update OPENAI_API_KEY with your key
#
# 3. Gmail Setup (Recommended for Development):
#    - Enable 2-Factor Authentication on your Google account
#    - Generate App-Specific Password:
#      a. Go to https://myaccount.google.com/apppasswords
#      b. Select "Mail" and your device
#      c. Copy the 16-character password
#    - Enable IMAP in Gmail Settings > Forwarding and POP/IMAP
#    - Update EMAIL_USER, EMAIL_PASSWORD, IMAP_USER, IMAP_PASSWORD
#
# 4. Alternative Email Services:
#    - SendGrid: Sign up at https://sendgrid.com/, get API key
#    - AWS SES: Set up in AWS Console, get access keys
#    - Update corresponding configuration variables
#
# 5. Email Polling:
#    - Set ENABLE_EMAIL_POLLING=true to automatically check for vendor responses
#    - Adjust EMAIL_POLLING_INTERVAL based on your needs (default: 5 minutes)
#
# ============================================
